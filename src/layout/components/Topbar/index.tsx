import { defineComponent } from "vue";
import { ElHeader } from "element-plus";

export default defineComponent({
  name: "Topbar",
  setup() {
    return () => {
      return <div class="h-12 bg-white">KK</div>;
    };
  }
});
