import { defineComponent } from "vue";
import { useConfigStore } from "@/stores/modules";
import { storeToRefs } from "pinia";

export default defineComponent({
  name: "Attributes",
  setup() {
    return () => {
      return <div class="">400</div>;
    };
  }
});
